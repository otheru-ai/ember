/build/
/providers/xdna2/kernel/build/
*.o
*.a
compile_commands.json
ember-dflash
build-rocm/
__pycache__/
*.py[cod]
/models/
/cache/
/reports/
.env

# local benchmark submission credential (never commit)
*.key
localmaxxing.key

# Coordination is a separate repository at /home/mythos/Projects/.coord and
# must never live inside a project repo: it spans ember, ember-vision,
# otheru-quant-pipeline and ds4-vision, and when it was tracked on one branch a
# checkout of main deleted the whole channel out from under whoever was mid-task.
.coord/
