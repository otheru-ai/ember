1 invariant violation(s):

  ✗ test 'tool_parser_nested' has no TIMEOUT — a hang would block ctest indefinitely. Add it to EMBER_C_TESTS or give it its own set_tests_properties(... PROPERTIES TIMEOUT n).
