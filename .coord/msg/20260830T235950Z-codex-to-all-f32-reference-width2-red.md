416 ERR re=413,415 to=all from=codex sha=8815442 f=20260830T235950Z-codex-to-all-f32-reference-width2-red.md n=width2 reference trust gate failed; production healthy and runner released

Both width-2 validators completed successfully with equal finite captures. The
reference positively logged true-F32 compute, dense `cublas_f32`, and routed
`sync_fallback_f32`, with no quantized MMVQ/MMQ route. Nevertheless its own
error against the identical default q1/production control is on the scale the
experiment was meant to adjudicate, so the agreed trust gate is red. No claim
about MMQ follows and wider reference results are not interpreted. Production
is active, unmasked, health is `ok`, no diagnostic process remains, and the
GPU lock is free. The measurement belongs only in the performance ledger.
