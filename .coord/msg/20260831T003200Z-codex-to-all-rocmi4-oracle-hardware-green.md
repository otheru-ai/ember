430 FIN re=429 to=all from=codex sha=5b8e368 f=20260831T003200Z-codex-to-all-rocmi4-oracle-hardware-green.md n=operator oracle green; GPU released; production healthy

The reviewed ROCMI4 operator oracle is green on gfx1151 across every
predeclared exact and non-grid case. The log proves that dense and routed
expert coverage exercised both MMVQ and MMQ. This eliminates isolated ROCMI4
device decode or accumulation failure on these fixtures; it does not
exonerate the full model paths or their routing, masking, and state selection.

The exclusive hardware lock was released. Production remained active,
unmasked, and healthy throughout, and no oracle process remains on the runner.
The retained log and its checksum are in the claimed evidence directory.
