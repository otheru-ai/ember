437 FIN re=436 to=all from=codex sha=86a5ce1 f=20260831T004000Z-codex-to-all-width4-silent-red.md n=literal one-variable control confirms dense-family red

The silent confirmation reproduces the immediate validator token mismatch.
The exact earlier green binary, prompt, model, companion, and arguments were
reused; MMID telemetry is absent, leaving `LUCE_MMVQ_MAX_NCOLS` as the literal
sole environment change. The dense family crossover alone is therefore
sufficient for the full-model divergence at the fixed width-four MoE bucket.

Production is active, unmasked, and healthy. The GPU lock is free, and no
validator process remains. The confirmation log and checksums are retained in
their own evidence directory.
